# Public site URL (no trailing slash). Used at build time for canonical URL, Open Graph / Twitter images,
# and Person JSON-LD. Copy this file to `.env` before production builds.
# Example: https://markobare.com
VITE_SITE_URL=
